{{!--
<h1>Eat dis burger foo!</h1>
<h2>Burgers to be devoured</h2>
<ul>
  {{#each burgers}} {{#unless this.devoured}}
  <h3>{{this.id}} {{this.burger_name}}
    <button class="eat-burger" data-id="{{this.id}}">Eat me!</button>
  </h3>
  {{/unless}} {{/each}}
</ul>
<h2>Burgers eaten up</h2>

<ul>
  {{#each burgers}} {{#if this.devoured}}
  <h3>{{this.id}} {{this.burger_name}}</h3>
  {{/if}} {{/each}}
</ul>
<h2>Add a burgie</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgerInput">Burger:</label>
    <input type="text" id="burgerInput" name="burgerName">
  </div>

  <br>
  <button type="submit" id="addBurger">Add Burger</button> --}}

</form>
<div class="jumbotron jumbotron-fluid">
  <div class="container">
    <h1 class="display-4">
      <img src="https://cdn.pixabay.com/photo/2016/09/16/19/13/burger-1674881_1280.png" height="200px" width="200px">Devour the Burger!</h1>
    <h2>Eat all you want, no calories!</h2>
  </div>
</div>
<div class="row">
  <div class="col-md-6">
    <h2>Burgers to be devoured</h2>
    <hr>
    <ul>
      {{#each burgers}} {{#unless this.devoured}}
      <h3>{{this.id}} {{this.burger_name}}
        <button class="eat-burger" data-id="{{this.id}}">Eat me!</button>
      </h3>
      {{/unless}} {{/each}}
    </ul>

  </div>
  <div class="col-md-6">

    <h2>Burgers eaten up</h2>
    <hr>
    <ul>
      <ul>
        {{#each burgers}} {{#if this.devoured}}
        <h3>{{this.id}} {{this.burger_name}}</h3>
        {{/if}} {{/each}}
      </ul>
    </ul>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <h2>Add a burgie</h2>
    <form class="create-form">

      <div class="form-group">
        <label for="burgerInput">Burger:</label>
        <input type="text" id="burgerInput" name="burgerName">
      </div>

      <br>
      <button type="submit" id="addBurger">Add Burger</button>

    </form>

  </div>
</div>